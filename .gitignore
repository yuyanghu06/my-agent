# OS
.DS_Store

# Node
node_modules/
*.log
npm-debug.log*

# Builds / dist
build/dist/
raycast/dist/
raycast/.raycast/
spotlight/dist/
spotlight/src-tauri/target/
spotlight/src-tauri/gen/

# Logs
logs/

# Runtime user data — populated by the running spotlight app, not source
spotlight-clips/
spotlight-files/
spotlight-images/
# spotlight-sessions/ is tracked — session history + settings live here

# Generated/per-machine LaunchAgent (use bin/install.sh to regenerate)
com.*.agent.plist

# Credentials — never commit
.env
.env.*
*.pem
creds-*.json
